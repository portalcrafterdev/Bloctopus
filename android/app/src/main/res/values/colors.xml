<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The scaffold colour. The launch window uses it so the app never
         flashes a different colour on the way in. Keep this in step with `bg`
         in lib/app/theme.dart. -->
    <color name="blocktopus_bg">#FF8259C6</color>
</resources>
